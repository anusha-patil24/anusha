#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <math.h>
int *q;
int max, rear = -1, front = 0, count = 0;
void enqueue() {
    int ele;
    if(count == max) {
        printf("Queue Overflow");
    } else {
        
    }
}